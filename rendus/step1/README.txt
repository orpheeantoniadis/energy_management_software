We use a PLPGSQL database, you need to have the same. 

Once installed, you should run the distributed_create.sql script in bdd folder for creating the database.

The other script is just a example of insertion into the database.